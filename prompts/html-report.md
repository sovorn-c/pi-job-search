---
description: Generate an offline job-search report
argument-hint: "[output path]"
---
Use `job_search_html_report` to generate a self-contained offline dashboard from local tracker/outcome state. Escape all values, include status/sector/search filters and funnel metrics, and do not load external scripts, styles, fonts, or data. Arguments: $ARGUMENTS
