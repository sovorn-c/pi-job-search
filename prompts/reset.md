---
description: Reset the job-search workspace after exact confirmation
argument-hint: "[confirm]"
---
Preview the selected reset mode (`profile`, `documents`, or `all`) and list affected paths. Execute only when the user types the literal `RESET`; otherwise make no changes. Keep all deletion bounded to `.pi-job-search/`. Arguments: $ARGUMENTS
