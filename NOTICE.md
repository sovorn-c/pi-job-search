# Notices

`pi-job-search` is an independent clean-room implementation. Its workflow concepts were informed by the observable behavior and documentation of the MIT-licensed `ai-job-search` project; no source code is copied and no source-code identity is claimed.

This package depends on third-party packages listed in `package-lock.json`, each under its own license. `npm run verify:security` checks that local state and credentials are not included in release artifacts.
